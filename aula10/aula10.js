const nome = prompt("qual seu nome?")
const email = prompt('qual seu email?')

const fraseTemplate = `o e-mail ${email} foi cadastrado com sucesso. Seja bem-vinda(o), ${nome}!`


console.log(fraseTemplate)